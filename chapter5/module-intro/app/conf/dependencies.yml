# Application dependencies
require:
    - play
    - customModules -> firstmodule 

repositories:
    - playCustomModules:
        type:       local
        artifact:   "/tmp/play/firstmodule/"
        contains:
            - customModules -> *
